extends Node

signal player_health_change(maxValue: float, currValue: float)
signal player_mana_change(maxValue: float, currValue: float)
signal focus_enemy(label: String, maxHealth: float, currHealth: float)
